# Kiro × Copilot チートシート

すぐに使える判断基準とコマンド集

## 🤔 どっちを使う？

### Kiroに依頼する場合

```
✅ 新機能の追加
✅ ファイル構造の変更
✅ 複数ファイルの変更
✅ 複雑なロジック
✅ バグの診断
✅ アーキテクチャの変更
✅ パッケージの追加
✅ 設定ファイルの変更
```

**依頼例**:
```
「区ごとのページ機能を追加して」
「Supabaseに移行して」
「パフォーマンスを最適化して」
「このバグの原因を診断して」
```

### Copilotで自分でやる場合

```
✅ データの追加・修正
✅ スタイルの調整
✅ 文言の変更
✅ 小さなバグ修正
✅ コメントの追加
✅ テストの作成
✅ 1ファイルの変更
```

**使い方**:
```
Cmd + I → 「このスタイルを改善して」
コメント → 「会場データを追加」→ Tab
```

---

## ⌨️ Copilot ショートカット

| 操作 | Mac | Windows |
|------|-----|---------|
| **Copilot Chat** | `Cmd + I` | `Ctrl + I` |
| 補完を受け入れ | `Tab` | `Tab` |
| 補完を拒否 | `Esc` | `Esc` |
| 次の提案 | `Option + ]` | `Alt + ]` |
| 前の提案 | `Option + [` | `Alt + [` |
| サイドバーChat | クリック | クリック |

---

## 💬 Copilot Chat コマンド

```
/explain  - コードの説明
/fix      - バグ修正
/tests    - テスト作成
/doc      - ドキュメント生成
```

**使用例**:
```
/explain このコンポーネントは何をしている？
/fix このエラーを修正して
/tests このコンポーネントのテストを作成
/doc この関数のJSDocを追加
```

---

## 🔄 典型的なワークフロー

### パターン1: 新機能

```
1. Kiro → 「〜機能を追加して」
2. 確認 → ブラウザで動作確認
3. Copilot → スタイル調整、文言変更
4. コミット → git commit
```

### パターン2: データ追加

```
1. Kiro → 「データフォーマットを作成して」
2. Copilot → 同じフォーマットで追加
3. 確認 → ブラウザで確認
4. コミット → git commit
```

### パターン3: バグ修正

```
1. Copilot → エラーメッセージを貼り付け
2. 解決しない → Kiroに相談
3. Kiro → 根本原因を診断・修正
4. Copilot → 微調整
```

---

## 📝 よく使うCopilot指示

### データ追加
```
「以下のデータを追加して：
施設名: 〜
住所: 〜
...」
```

### スタイル変更
```
「このカードをもっと魅力的にして」
「ホバー時に影を濃くして」
「レスポンシブ対応して」
```

### バグ修正
```
「このエラーを修正して：
[エラーメッセージ]」
```

### リファクタリング
```
「この関数を読みやすくして」
「重複コードをまとめて」
```

---

## 🎯 Kiroへの効果的な依頼

### 良い依頼例

```
✅ 「区ごとのページ機能を追加してください。
   URL: /wards/[ward]
   表示内容: 会場一覧、統計情報
   必要なファイル: page.tsx, 型定義」

✅ 「地図表示機能を追加してください。
   使用API: Google Maps
   表示場所: 詳細ページ
   機能: マーカー表示、ズーム」
```

### 悪い依頼例

```
❌ 「いい感じにして」
   → 具体的に何をしたいか不明

❌ 「色を変えて」
   → Copilotで自分でできる
```

---

## 🔧 トラブル時の対処

### Copilotが動かない

```bash
1. VS Code再起動
2. 拡張機能の確認
3. GitHubサインイン確認
```

### エラーが出た

```
1. Copilot Chat → エラーを貼り付け
2. 解決しない → Kiroに相談
```

### フォーマットが崩れた

```bash
npx prettier --write .
```

---

## 📂 ファイル別の作業分担

| ファイル | Kiro | Copilot |
|---------|------|---------|
| `venues.json` | 構造設計 | データ追加 |
| `page.tsx` | 新規作成 | 微調整 |
| `components/*.tsx` | 新規作成 | スタイル調整 |
| `types/*.ts` | 型定義 | 追加・修正 |
| `README.md` | - | 文言修正 |

---

## 🎨 スタイル調整のテンプレート

### Copilot Chatに貼り付け

```
このコンポーネントのスタイルを改善してください：

要件:
- ホバー時に影を濃くする
- 角を丸くする（rounded-lg）
- パディングを増やす
- 色をブランドカラー（青系）に
```

---

## 🐛 デバッグのテンプレート

### Copilot Chatに貼り付け

```
以下のエラーを修正してください：

エラーメッセージ:
[エラーをコピペ]

発生箇所:
[ファイル名と行番号]

期待する動作:
[どうなってほしいか]
```

---

## 📊 コミットメッセージ

```bash
# Kiroで実装
git commit -m "feat: 区ごとのページ機能を追加 (Kiro)"

# Copilotで調整
git commit -m "style: カードデザインを改善 (Copilot)"
git commit -m "feat: 区の説明文を追加 (Copilot)"

# Kiroで修正
git commit -m "fix: パフォーマンス最適化 (Kiro)"
```

---

## 🚀 クイックスタート

### 開発を始める

```bash
cd ~/.kiro/concert-venues
code .
npm run dev
```

### 最初のCopilot体験

1. `public/data/venues.json` を開く
2. `Cmd + I` を押す
3. 「このファイルについて説明して」と入力
4. Copilotの回答を確認

### 最初のデータ追加

1. `venues.json` の最後に移動
2. `Cmd + I` を押す
3. 「以下のデータを追加して：[施設情報]」
4. 生成されたコードを確認して Accept

---

## 📚 詳細ドキュメント

- **詳しい使い方**: `DEVELOPMENT_GUIDE.md`
- **連携方法**: `KIRO_COPILOT_WORKFLOW.md`
- **クイックリファレンス**: `QUICK_REFERENCE.md`

---

## 💡 覚えておくこと

1. **大きな変更 = Kiro**
2. **小さな調整 = Copilot**
3. **困ったら = Kiro**
4. **学びながら = Copilot**

---

**このチートシートを印刷して手元に置いておくと便利です！ 📄**
